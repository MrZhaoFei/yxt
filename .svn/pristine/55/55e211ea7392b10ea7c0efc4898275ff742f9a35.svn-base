package org.system.controller.iface.service.visits;

import java.util.Map;

import org.springframework.validation.BindingResult;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestHeader;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.ResponseBody;
import org.system.entity.service.visits.GxyFollowupFzcheckRecords;
import org.utils.spring.Groups;

@RequestMapping()
public interface IGxyFollowupFzcheckRecordsController {

	/**
	 * 新增高血压随访辅助检查记录
	 * 
	 * @param gxyFollowupRecords
	 * @param result
	 * @param token
	 * @return
	 */
	@RequestMapping(value = { "/doctor/gxyFollowupFzcheckRecords" }, method = RequestMethod.POST)
	@ResponseBody
	public Map<String, Object> insertGxyFollowupFzcheckRecords(
			@Validated({ Groups.Insert.class }) GxyFollowupFzcheckRecords gxyFollowupFzcheckRecords,
			BindingResult result, @RequestHeader(value = "token", required = false) String token);
	
	/**
	 * 修改高血压随访 辅助检查记录
	 * @param gxyFollowupDragRecordsId
	 * @param gxyFollowupDragRecords
	 * @param result
	 * @param token
	 * @return
	 */
	@RequestMapping(value = { "/doctor/gxyFollowupFzcheckRecords/{recordId}" }, method = RequestMethod.PUT)
	@ResponseBody
	public Map<String, Object> updateGxyFollowupFzcheckRecords(@PathVariable("recordId") Integer gxyFollowupFzcheckRecordsId,
			@Validated({ Groups.Update.class }) GxyFollowupFzcheckRecords gxyFollowupFzcheckRecords, BindingResult result,
			@RequestHeader(value = "token", required = false) String token);
	

	/**
	 * 删除 高血压随访 辅助检查记录
	 * @param gxyFollowupDragRecordsId
	 * @param gxyFollowupDragRecords
	 * @param result
	 * @param token
	 * @return
	 */
	@RequestMapping(value = { "/doctor/gxyFollowupFzcheckRecords/{recordId}" }, method = RequestMethod.DELETE)
	@ResponseBody
	public Map<String, Object> deleteGxyFollowupFzcheckRecords(@PathVariable("recordId") Integer gxyFollowupFzcheckRecordsId,
			GxyFollowupFzcheckRecords gxyFollowupFzcheckRecords, BindingResult result,
			@RequestHeader(value = "token", required = false) String token);
	
}
