package org.system.controller.impl.sys;

import java.util.HashMap;
import java.util.List;
import java.util.Map;

import javax.annotation.Resource;

import org.core.result.ResultCode;
import org.core.result.ResultMap;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Controller;
import org.springframework.validation.BindingResult;
import org.system.Global;
import org.system.controller.iface.sys.ISysAboutController;
import org.system.entity.sys.SysAbout;
import org.system.message.Message;
import org.system.service.iface.sys.ISysAboutService;

import net.sf.ehcache.CacheManager;

@Controller
public class SysAboutController implements ISysAboutController {
	Logger log = LoggerFactory.getLogger(SysAboutController.class);

	@Resource
	private ISysAboutService sysAboutService;
	@Resource
	private CacheManager cache;

	@Override
	public Map<String, Object> getSysAboutList(SysAbout sysAbout, BindingResult result, String token) {
		// 缓存中获取用户数据
/*		Element element = cache.getCache(Global.CACHE_USER).get(token);
		if (element != null) {*/
			// 定义返回对象
			Map<String, Object> resultMap = new HashMap<>();
			// 根据用户id查询用户的服务项
			List<Map<String, Object>> aboutList = sysAboutService.getSysAboutList(sysAbout);
			if (aboutList != null && aboutList.size() > 0) {
				resultMap.put(Global.DATA_TOTAL, sysAbout.getTotal());
				resultMap.put(Global.DATA_ROWS, aboutList);
				return ResultMap.convertMap(ResultCode.CODE_SUCCESS, resultMap, Message.bundle("query.success"));
			}
			return ResultMap.convertMap(ResultCode.CODE_FAIL, Message.bundle("query.empty"));
		/*} else {
			return ResultMap.convertMap(ResultCode.CODE_NO_LOGIN, Message.bundle("permission.no.login"));
		}*/
	}

	@Override
	public Map<String, Object> getSysAboutDetail(Integer id, SysAbout sysAbout, BindingResult result, String token) {
		// 缓存中获取用户数据
		/*Element element = cache.getCache(Global.CACHE_USER).get(token);
		if (element != null) {*/
			sysAbout.setId(id);
			// 定义返回对象
			Map<String, Object> aboutMap = sysAboutService.getSysAboutDetail(sysAbout);
			if (aboutMap != null && aboutMap.size() > 0) {
				return ResultMap.convertMap(ResultCode.CODE_SUCCESS, aboutMap, Message.bundle("query.success"));
			}
			return ResultMap.convertMap(ResultCode.CODE_FAIL, Message.bundle("query.empty"));
	/*	} else {
			return ResultMap.convertMap(ResultCode.CODE_NO_LOGIN, Message.bundle("permission.no.login"));
		}*/
	}

}
