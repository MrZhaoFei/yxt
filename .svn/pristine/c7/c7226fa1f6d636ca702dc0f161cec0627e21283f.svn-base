package org.system.controller.iface.userService;

import java.util.Map;

import javax.servlet.http.HttpServletRequest;

import org.springframework.validation.BindingResult;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.ResponseBody;
import org.system.entity.userService.UserServicePack;
import org.utils.spring.Groups;

@RequestMapping(value = { "/admin" })
public interface IUserServicePackController {
	/**
	 * 所有的
	 * @param userServicePack
	 * @param result
	 * @return
	 */
	@RequestMapping(value = "/userServicePack", method = RequestMethod.GET)
	@ResponseBody
	public Map<String, Object> getUserServicePackList(@Validated({ Groups.Select.class }) UserServicePack userServicePack, BindingResult result,HttpServletRequest req);
	
}
