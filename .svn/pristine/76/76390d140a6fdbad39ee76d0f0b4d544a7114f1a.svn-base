package org.system.controller.impl.doctor;

import java.util.HashMap;
import java.util.List;
import java.util.Map;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpSession;

import org.core.result.ResultCode;
import org.core.result.ResultMap;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Controller;
import org.springframework.validation.BindingResult;
import org.system.Global;
import org.system.controller.iface.doctor.IDoctorGroupTypeController;
import org.system.entity.doctor.DoctorGroupType;
import org.system.message.Message;
import org.system.service.iface.doctor.IDoctorGroupTypeService;


@Controller
public class DoctorGroupTypeController implements IDoctorGroupTypeController {
	Logger log = LoggerFactory.getLogger(DoctorGroupTypeController.class);
	
	@Resource
	private IDoctorGroupTypeService doctorGroupTypeService;
	@Override
	public Map<String, Object> getDoctorList(DoctorGroupType doctorGroupType, BindingResult result,HttpServletRequest req) {
		 HttpSession session=req.getSession();
		 if(session.getAttribute("user")!=null){
			 Map<String, Object> resultMap = new HashMap<>();
			 //得到返回数据
			 List<Map<String, Object>> dataList = doctorGroupTypeService.getDoctorGroupTypeList(doctorGroupType);
			 
			 if (dataList != null && dataList.size() > 0) {
				 //得到数据总数
				 resultMap.put(Global.DATA_TOTAL, doctorGroupType.getTotal());
				 resultMap.put(Global.DATA_ROWS, dataList);
				 return ResultMap.convertMap(ResultCode.CODE_SUCCESS, resultMap, Message.bundle("query.success"));
			 }
			 return ResultMap.convertMap(ResultCode.CODE_DATA_EMPTY, Message.bundle("query.empty"));
			 
		 }else{
			 return ResultMap.convertMap(ResultCode.CODE_NO_LOGIN, Message.bundle("permission.no.login"));
		 }
	}

}
