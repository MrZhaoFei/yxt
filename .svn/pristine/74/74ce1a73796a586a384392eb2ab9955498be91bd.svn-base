package org.system.controller.iface.service.visits;

import java.util.Map;

import org.springframework.validation.BindingResult;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.RequestHeader;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.ResponseBody;
import org.system.entity.service.visits.TnbFollowupFzcheckRecords;
import org.utils.spring.Groups;

@RequestMapping()
public interface ITnbFollowupFzcheckRecordsController {

	/**
	 * 新增糖尿病随访辅助检查记录
	 * 
	 * @param gxyFollowupRecords
	 * @param result
	 * @param token
	 * @return
	 */
	@RequestMapping(value = { "/doctor/tnbFollowupFzcheckRecords" }, method = RequestMethod.POST)
	@ResponseBody
	public Map<String, Object> insertTnbFollowupFzcheckRecords(
			@Validated({ Groups.Insert.class }) TnbFollowupFzcheckRecords tnbFollowupFzcheckRecords,
			BindingResult result, @RequestHeader(value = "token", required = false) String token);
	
}
