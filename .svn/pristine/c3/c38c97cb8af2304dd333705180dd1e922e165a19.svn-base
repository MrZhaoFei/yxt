package org.system.controller.iface.service.visits;

import java.util.Map;

import org.springframework.validation.BindingResult;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestHeader;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.ResponseBody;
import org.system.entity.service.visits.TnbFollowupDragRecords;
import org.utils.spring.Groups;

@RequestMapping()
public interface ITnbFollowupDragRecordsController {

	/**
	 * 新增糖尿病随访用药记录
	 * 
	 * @param gxyFollowupRecords
	 * @param result
	 * @param token
	 * @return
	 */
	@RequestMapping(value = { "/doctor/tnbFollowupDragRecords" }, method = RequestMethod.POST)
	@ResponseBody
	public Map<String, Object> insertTnbFollowupDragRecords(
			@Validated({ Groups.Insert.class }) TnbFollowupDragRecords tnbFollowupDragRecords, BindingResult result,
			@RequestHeader(value = "token", required = false) String token);
	
	/**
	 * 修改糖尿病随访 用药 记录
	 * @param tnbFollowupDragRecordsId
	 * @param tnbFollowupDragRecords
	 * @param result
	 * @param token
	 * @return
	 */
	@RequestMapping(value = { "/doctor/tnbFollowupDragRecords/{recordId}" }, method = RequestMethod.PUT)
	@ResponseBody
	public Map<String, Object> updateTnbFollowupDragRecords(@PathVariable("recordId") Integer tnbFollowupDragRecordsId,
			@Validated({ Groups.Update.class })TnbFollowupDragRecords tnbFollowupDragRecords, BindingResult result,
			@RequestHeader(value = "token", required = false) String token);
	

	/**
	 * 删除糖尿病随访 用药 记录
	 * @param tnbFollowupDragRecordsId
	 * @param tnbFollowupDragRecords
	 * @param result
	 * @param token
	 * @return
	 */
	@RequestMapping(value = { "/doctor/tnbFollowupDragRecords/{recordId}" }, method = RequestMethod.DELETE)
	@ResponseBody
	public Map<String, Object> deleteTnbFollowupDragRecords(@PathVariable("recordId") Integer tnbFollowupDragRecordsId,
			TnbFollowupDragRecords tnbFollowupDragRecords, BindingResult result,
			@RequestHeader(value = "token", required = false) String token);
}
